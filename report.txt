// File: CreNumberCards.tsx
import React from 'react';
import { Grid, Typography, Box, Divider } from '@mui/material';
import CreNumberCard from './CreNumberCard';
import { GREEN_COLOR, RED_COLOR, BLUE_COLOR } from '@constants/Color_constant';

const CreNumberCards = () => {
  const jobSummary = [
    { title: 'Total runs', count: 2129, color: BLUE_COLOR },
    { title: 'Running', count: 0, color: BLUE_COLOR },
    { title: 'Canceled', count: 0, color: BLUE_COLOR },
    { title: 'Successful runs', count: 2005, color: GREEN_COLOR },
    { title: 'Failed runs', count: 124, color: RED_COLOR },
    { title: 'Run success rate', count: '94%', color: GREEN_COLOR },
    { title: 'DPU hours', count: 504, color: BLUE_COLOR },
  ];

  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h6" sx={{ marginBottom: 2 }}>
        Job runs summary
      </Typography>
      <Grid container alignItems="center" spacing={2}>
        {jobSummary.map((item, index) => (
          <React.Fragment key={index}>
            <Grid item>
              <CreNumberCard title={item.title} count={item.count} color={item.color} />
            </Grid>
            {index < jobSummary.length - 1 && (
              <Divider
                orientation="vertical"
                flexItem
                sx={{
                  marginLeft: 1,
                  marginRight: 1,
                  height: 'auto',
                  borderWidth: 1,
                  backgroundColor: '#e0e0e0',
                }}
              />
            )}
          </React.Fragment>
        ))}
      </Grid>
    </Box>
  );
};

export default CreNumberCards;
