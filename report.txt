import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';
import { useContext } from 'react';
import { CreContext } from './CreContext'; // Adjust the path as needed

// Reusable StatCard component
const StatCard = ({ title, size, onClick }) => {
  const borderColor = size === 0 ? 'green' : 'gray'; // Dynamic border color

  return (
    <Card 
      sx={{ minWidth: 275, border: `2px solid ${borderColor}`, cursor: 'pointer' }}
      onClick={onClick}
    >
      <CardContent>
        <Typography variant="h6">
          {title} ({size})
        </Typography>
      </CardContent>
    </Card>
  );
};

export default StatCard;
