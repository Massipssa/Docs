 const handleCreClick = (creName) => {
    const filtered = projectData.filter(project =>
      ['critical', 'received', 'notCritical', 'notExpectedReceived'].some(field =>
        project[field]?.some(cre => cre.name === creName)
      )
    );
    setFilteredProjects(filtered);
  };
