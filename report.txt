import React from 'react';
import { Box, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Paper, Typography } from '@mui/material';
import CheckBreadcrumbAndFilters from './CheckBreadcrumbAndFilters';

// Mock data for demonstration
const data = [
  { id: 1, name: 'Item 1', status: 'Ok', date: '01/09/2023', severity: 'low' },
  { id: 2, name: 'Item 2', status: 'Error', date: '02/09/2023', severity: 'high' },
];

const DataCheck = () => {
  return (
    <Box>
      <CheckBreadcrumbAndFilters checkType="Data Check" />
      <TableContainer component={Paper}>
        <Table>
          <TableHead>
            <TableRow>
              <TableCell>ID</TableCell>
              <TableCell>Name</TableCell>
              <TableCell>Status</TableCell>
              <TableCell>Date</TableCell>
              <TableCell>Severity</TableCell>
            </TableRow>
          </TableHead>
          <TableBody>
            {data.map((row) => (
              <TableRow key={row.id}>
                <TableCell>{row.id}</TableCell>
                <TableCell>{row.name}</TableCell>
                <TableCell>{row.status}</TableCell>
                <TableCell>{row.date}</TableCell>
                <TableCell>{row.severity}</TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      </TableContainer>
    </Box>
  );
};

export const MetadataCheck = () => (
  <Box>
    <CheckBreadcrumbAndFilters checkType="Metadata Check" />
    <Typography variant="h6">Metadata Check Table will be displayed here.</Typography>
  </Box>
);

export const ParameterCheck = () => (
  <Box>
    <CheckBreadcrumbAndFilters checkType="Parameter Check" />
    <Typography variant="h6">Parameter Check Table will be displayed here.</Typography>
  </Box>
);

export const MonitoringCheck = () => (
  <Box>
    <CheckBreadcrumbAndFilters checkType="Monitoring Check" />
    <Typography variant="h6">Monitoring Check Table will be displayed here.</Typography>
  </Box>
);

export default DataCheck;
