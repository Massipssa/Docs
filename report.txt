<Grid container spacing={2}>
  {data.map((item, index) => (
    <Grid item xs={4} key={index}>
      <Card
        sx={{
          height: 150, // Set consistent height for all cards
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          textAlign: 'center',
          padding: 2, // Add padding for better text layout
        }}
      >
        <CardContent sx={{ flexGrow: 1 }}>
          <Typography
            variant="h6"
            sx={{
              whiteSpace: 'normal', // Allow text to wrap
              wordWrap: 'break-word', // Break long words
              fontSize: '1rem',
              lineHeight: '1.2',
              overflow: 'hidden', // Handle overflow in case text exceeds card size
              display: '-webkit-box',
              WebkitLineClamp: 3, // Limit to 3 lines
              WebkitBoxOrient: 'vertical',
            }}
          >
            {item.title}
          </Typography>
        </CardContent>
      </Card>
    </Grid>
  ))}
</Grid>
