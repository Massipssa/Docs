cid=$(docker create oa-mask ... )
docker cp install/storage/. "$cid":/app/oa/EllipsysStorage/openAudit
docker cp install/oa.env "$cid":/app/oa.env
docker start -a "$cid"
docker cp "$cid":/app/oa/EllipsysStorage/openAudit/output/. install/output/
docker rm "$cid"
