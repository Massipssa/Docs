import React, { useState } from 'react';
import { Container, Box, Typography, MenuItem, FormControl, InputLabel, Select, TextField, Button, Grid } from '@mui/material';
import { DatePicker } from '@mui/x-date-pickers/DatePicker';

const DataQualityCheck = () => {
  const [selectedCheck, setSelectedCheck] = useState('');
  const [selectedDate, setSelectedDate] = useState(null);
  const [selectedSeverity, setSelectedSeverity] = useState('');

  const handleCheckChange = (event) => {
    setSelectedCheck(event.target.value);
  };

  const handleDateChange = (date) => {
    setSelectedDate(date);
  };

  const handleSeverityChange = (event) => {
    setSelectedSeverity(event.target.value);
  };

  const handleSubmit = () => {
    // You can implement the logic to perform the data quality check here
    console.log('Selected Check:', selectedCheck);
    console.log('Selected Date:', selectedDate);
    console.log('Selected Severity:', selectedSeverity);
  };

  return (
    <Container maxWidth="sm">
      <Box p={3} textAlign="center">
        <Typography variant="h4" gutterBottom>
          Data Quality Check
        </Typography>

        <Grid container spacing={2}>
          {/* Select Check Type */}
          <Grid item xs={12}>
            <FormControl fullWidth>
              <InputLabel id="check-label">Select Check</InputLabel>
              <Select
                labelId="check-label"
                value={selectedCheck}
                onChange={handleCheckChange}
                label="Select Check"
              >
                <MenuItem value="data">Data Check</MenuItem>
                <MenuItem value="metadata">Metadata Check</MenuItem>
                <MenuItem value="parameter">Parameter Check</MenuItem>
                <MenuItem value="monitoring">Monitoring Check</MenuItem>
              </Select>
            </FormControl>
          </Grid>

          {/* Select Date */}
          <Grid item xs={12}>
            <DatePicker
              label="Select Date"
              value={selectedDate}
              onChange={handleDateChange}
              renderInput={(params) => <TextField fullWidth {...params} />}
            />
          </Grid>

          {/* Select Severity */}
          <Grid item xs={12}>
            <FormControl fullWidth>
              <InputLabel id="severity-label">Select Severity</InputLabel>
              <Select
                labelId="severity-label"
                value={selectedSeverity}
                onChange={handleSeverityChange}
                label="Select Severity"
              >
                <MenuItem value="low">Low</MenuItem>
                <MenuItem value="medium">Medium</MenuItem>
                <MenuItem value="high">High</MenuItem>
                <MenuItem value="critical">Critical</MenuItem>
              </Select>
            </FormControl>
          </Grid>

          {/* Submit Button */}
          <Grid item xs={12}>
            <Button
              variant="contained"
              color="primary"
              onClick={handleSubmit}
              fullWidth
            >
              Perform Check
            </Button>
          </Grid>
        </Grid>
      </Box>
    </Container>
  );
};

export default DataQualityCheck;
