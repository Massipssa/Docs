export const ClickableProjectCard = ({
  item,
  onClick,
  showActions,
  displayProjectDescription,
  critical,
  clickedCard,
}) => {
  return (
    <Card
      variant="outlined"
      key={item.name}
      onClick={() => onClick(item.name)}
      sx={{
        boxShadow: 'none',
        display: 'flex',
        justifyContent: 'space-between', // Ensure space between icon and text
        alignItems: 'center',
        textAlign: 'center', // Center text horizontally
        width: '100%',
        height: 180,
        border: '1px solid',
        borderColor: clickedCard === item.name ? setColor(item, critical) : 'white',
        cursor: 'pointer',
      }}
    >
      {/* Icon aligned to the left */}
      {showActions && (
        <CardActions sx={{ justifyContent: 'flex-start' }}>
          <IconButton>
            <VisibilityOutlinedIcon />
          </IconButton>
        </CardActions>
      )}

      {/* Centered text */}
      <CardContent sx={{ flexGrow: 1 }}>
        <Typography
          variant="subtitle2"
          sx={{
            whiteSpace: 'normal',
            wordWrap: 'break-word', // Break long words
            lineHeight: '1.2em',
            overflow: 'hidden', // Hide overflow if text exceeds the card
            display: '-webkit-box',
            WebkitLineClamp: 3, // Limit to 3 lines
            WebkitBoxOrient: 'vertical',
            textAlign: 'center', // Ensures the text is centered vertically
          }}
        >
          {item.name}
        </Typography>
      </CardContent>
    </Card>
  );
};
